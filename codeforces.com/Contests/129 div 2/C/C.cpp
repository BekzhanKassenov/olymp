#include <iostream>
#include <cstdio>

using namespace std;

typedef long long ll;

vector <ll> ans

int main()
{
	ll l, r;
	cin >> l >> r;
	string ls = int_to_str(l), rs = int_to_str(r);
	int ml = ls.length() - 1;
	int mr = rs.length() - 1;
}