#include <iostream>
#include <cstdio>
#include <vector>

using namespace std;

int main()
{
	int n;
	cin >> n;
	int a[2];
	int p = 0;
	if (a[p] > a[p - 1])
		cout << "";
	else
		cout << 0 << ' ' << a[p - 1];
   return 0;	
}
