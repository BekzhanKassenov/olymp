Задача
Дано n отрезков и m запросов. Каждый запрос - точка,
с коориднатами (xi, 0), xi принимается из входного потока.
Для каждой точки надо найти отрезок, который имеет
наименьшую y-координату при пересечении с прямой xi.

1) Отрезки не пересекаются, оффлайн решение (Main1.cpp), онлайн
решение (Main2.cpp).
