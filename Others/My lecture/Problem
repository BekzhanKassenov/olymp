Given array of integers of size N <= 10^5;
Also given 10^5 queries
All queries - update on point and sum on range.
First number of query is type if query.
If type = 0 then it's update query.
	Second number is position, third number is value.
If type = 1 then it's get query.
	Second number is left and thrid number is right. 
Output sum on range for every query of type 1;

