#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

#define PII pair <int, int>
#define F first
#define S second
#define MP make_pair

int main()
{
	int n;
	cin >> n;

	vector <PII> a();

	for (int i = 0; i < n; i++)
		cin >> a[i].F >> a[i].S;

	sort(a.begin(), a.end());



}
